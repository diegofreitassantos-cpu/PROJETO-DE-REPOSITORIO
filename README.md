# 📁 Nome do Projeto

Descrição breve do projeto.

---

## 🗂️ Estrutura de Pastas

```
projeto-github/
│
├── 📹 videos/          → Vídeos do projeto (mp4, mov, avi…)
├── 🖼️  imagens/         → Imagens de interface, banners e ícones
├── 📷 fotos/           → Fotografias originais e referências
│
├── 💻 codigo/          → Código-fonte
│   ├── src/            → Arquivos principais
│   ├── components/     → Componentes reutilizáveis
│   ├── utils/          → Funções utilitárias
│   └── tests/          → Testes automatizados
│
├── 🔤 fonts/           → Tipografias e arquivos de fonte (ttf, woff…)
├── 📄 docs/            → Documentação, PDFs, apresentações
├── 🎨 assets/          → Outros arquivos de apoio
│
├── .gitignore          → Arquivos ignorados pelo Git
└── README.md           → Este arquivo
```

---

## 🚀 Como usar

```bash
# Clonar o repositório
git clone https://github.com/seu-usuario/nome-do-projeto.git

# Entrar na pasta
cd nome-do-projeto
```

---

## 📌 Convenções

| Pasta       | Formatos aceitos                     |
|-------------|--------------------------------------|
| `videos/`   | `.mp4` `.mov` `.avi` `.mkv`          |
| `imagens/`  | `.png` `.svg` `.webp` `.jpg`         |
| `fotos/`    | `.jpg` `.jpeg` `.raw` `.heic`        |
| `codigo/`   | `.js` `.ts` `.py` `.html` `.css` … |
| `fonts/`    | `.ttf` `.otf` `.woff` `.woff2`       |
| `docs/`     | `.pdf` `.md` `.docx`                 |

---

## 🤝 Contribuindo

1. Faça um fork do projeto
2. Crie sua branch: `git checkout -b minha-feature`
3. Commit suas mudanças: `git commit -m 'feat: minha feature'`
4. Push: `git push origin minha-feature`
5. Abra um Pull Request

---

*Projeto organizado com ❤️*
